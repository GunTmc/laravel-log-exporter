[Unit]
Description=Jadwal harian alogreport

[Timer]
# Sesudah tengah malam, untuk log kemarin yang sudah lengkap
OnCalendar=*-*-* 00:10:00
RandomizedDelaySec=120
# Bila mesin mati saat jadwal, jalankan begitu menyala lagi
Persistent=true

[Install]
WantedBy=timers.target
